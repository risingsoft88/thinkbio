<section class="cta2">
    <div class="cta2__inner">
        <h2>{{ title }}</h2>
        <p>{{ content }}</p>
        <div class="cta2__link">
            <a href="{{ link.url }}">{{ link.title }}</a>
        </div>
    </div>
</section>